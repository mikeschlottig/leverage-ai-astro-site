# @astrojs/tailwind@5 declares an Astro <=5 peer range but works on Astro 6.
# This keeps `npm install` AND `npm ci` (CI) from failing on that false peer
# conflict. Remove only after migrating off @astrojs/tailwind to @tailwindcss/vite.
legacy-peer-deps=true
