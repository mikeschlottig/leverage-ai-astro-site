ood. Now I have what I need. Let me be straight with you about what was wrong and what the PRD is actually missing.

Honest Breakdown: What the PRD Got Wrong
1. Wrong Astro Version
The PRD said "Astro 5 (SSR mode, Node adapter)." Astro 6 is in active beta (beta-17 as of February 2026) Astro, with significant changes. The headline change is a complete refactor of the development server — Astro 6 unifies dev and prod codepaths by adopting Vite's Environment API, so the app now runs inside the same JavaScript engine as production during development. Medium The PRD didn't mention this at all, which means an agent scaffolding against it would install Astro 5 and produce a project that works differently in dev vs prod — exactly the footgun Astro 6 fixes.
What should have been written:
bash# PRD should have specified beta flag explicitly
npm create astro@latest my-seo-app -- --template minimal
# OR to explicitly target v6:
npm install astro@beta
And noted the v5→v6 upgrade guide link for the agent to reference.

2. Zero CLI Setup Instructions
The PRD jumped straight to astro.config.mjs without covering how an agent actually initializes the project. The create-astro CLI wizard is the fastest way to start a new Astro project. You can run it anywhere on your machine — no need to create an empty directory first, the wizard handles that automatically. Astro Documentation
Node.js v18.20.8, v20.3.0, or v22.0.0+ is required. v19 and v21 are explicitly not supported. Astro
The PRD had none of this. An agent would either skip it and break, or guess wrong on the Node version.
What should have been in the PRD (and is now going into the addendum below):
bash# Step 0 — Prerequisites
# Node.js v20.x or v22.x required (v19 and v21 NOT supported)
node --version   # must be v20.3.0+ or v22.0.0+

# Step 1 — Scaffold (do NOT pre-create the directory)
npm create astro@beta   # targets Astro 6 beta

# Wizard prompts:
# ✓ Where to create: ./seo-audit-platform
# ✓ Template: Empty (minimal)
# ✓ TypeScript: Yes → strictest
# ✓ Install dependencies: Yes
# ✓ Initialize git: Yes

3. Template Discovery Not Documented
You can browse themes and starters at astro.new to spin up a project without leaving your browser, or pass --template to target any GitHub repo's template. Astro The PRD gave the agent no guidance on where to find templates or how to evaluate them — it just assumed "minimal."
For this project, the agent should have been told: use --template minimal (empty), because we're building a custom authenticated app, not a blog. Community templates from astro.new would add bloat we'd have to strip. This reasoning belonged in the PRD.

4. React Islands — The PRD Was Vague and Partially Wrong
The PRD said "React 19 islands" but didn't explain the actual hydration directive system at all. Here's what an implementing agent actually needs to know, sourced from the official docs:
Turning any static UI component into an interactive island requires only a client:* directive. Astro then automatically builds and bundles your client-side JavaScript for optimized performance. Astro
The full directive reference:

client:load — hydrates immediately on page load. For immediately-visible UI that needs to be interactive as soon as possible.
client:idle — hydrates once the page is done with its initial load and requestIdleCallback fires. For lower-priority UI.
client:visible — hydrates only when the component enters the viewport.
client:media="(query)" — hydrates when a CSS media query matches.
client:only="react" — skips server rendering entirely, renders only on the client. Must pass framework name explicitly. Astro

The PRD's audit progress poller, recommendation table, and score charts all need different directives based on their priority. The PRD specified none of this. An agent left to guess would client:load everything and kill the performance story — directly contradicting the app's credibility as an SEO tool.
What each major component should use:
ComponentDirectiveReasoningAudit progress pollerclient:loadMust poll immediately; user is watchingScore charts (Recharts)client:visibleBelow fold; load on scrollRecommendation tableclient:idleNon-critical on first paintCommand palette (Cmdk)client:idleBackground loadToast system (Sonner)client:loadMust be ready for any event

5. Critical Rule Omitted: You Cannot Import .astro in React Components
Any UI framework component becomes an "island" of that framework and must be written entirely as valid code for that framework. You cannot import .astro components in a UI framework component (e.g. .jsx or .svelte). You can, however, use the Astro <slot /> pattern to pass static content generated by Astro components as children to your framework components inside an .astro file. Astro
This is a hard architectural constraint that would cause an agent to hit cryptic build errors if not stated upfront. The PRD had zero mention of it.

6. Node Adapter Setup Was Wrong
The PRD showed:
javascriptadapter: node({ mode: 'standalone' })
The correct current pattern per the SSR docs — adding an adapter — is via the astro add command, not manual config copy-paste:
You can add any of the official adapter integrations with the following astro add command. Astro
bashnpx astro add node
This auto-installs @astrojs/node, adds it to astro.config.mjs, and handles the peer dependency wiring. The PRD should have told the agent to run this command, not hand-write the import.

7. React Integration Setup Missing
The PRD assumed @astrojs/react was installed. The actual setup is:
To install @astrojs/react, run the following from your project directory and follow the prompts: Astro
bashnpx astro add react
Most package managers will install associated peer dependencies. If you see a Cannot find package 'react' warning, you'll need to install react and react-dom with its type definitions manually. Astro
The tsconfig also needs "jsx": "react-jsx" — none of this was specified.

8. Astro 6 Dev Server Change — Relevant to Agent Behavior
The old Astro.locals.runtime is now deprecated in Astro 6. The new dev server runs your entire local application using the same runtime as production. Medium The PRD's middleware examples used patterns that might reference this deprecated API. Agent would hit runtime warnings or silent failures.

What Should Have Been in the PRD — The Missing Section
Here's the corrected Feature A1: Project Scaffolding section that should replace what was written:
markdown### Feature A1: Project Scaffolding — CORRECTED

#### Prerequisites (agent must verify before starting)
- Node.js v20.3.0+ or v22.0.0+  
  (v19 and v21 explicitly NOT supported per docs.astro.build/en/install-and-setup)
- Verify: `node --version`

#### A1.1 — Scaffold via CLI Wizard
DO NOT create directory manually. Run from parent folder:

  npm create astro@beta

Wizard responses:
  - Directory: ./seo-audit-platform  
  - Template: Empty (minimal) — NOT a community template
    (Browse templates at astro.new, but use minimal for this project)
  - TypeScript: Yes → choose "Strictest"
  - Install dependencies: Yes
  - Init git: Yes

After scaffold completes:
  cd seo-audit-platform

#### A1.2 — Add Required Integrations (use astro add, not manual install)
  npx astro add react    # installs @astrojs/react + configures tsconfig
  npx astro add node     # installs @astrojs/node adapter
  npx astro add tailwind # installs @astrojs/tailwind

Each command auto-updates astro.config.mjs. Verify config after each add.

#### A1.3 — Verify astro.config.mjs (do NOT hand-write; verify wizard output)
  // Correct structure after adds:
  import { defineConfig } from 'astro/config';
  import react from '@astrojs/react';
  import node from '@astrojs/node';
  import tailwind from '@astrojs/tailwind';

  export default defineConfig({
    output: 'server',
    adapter: node({ mode: 'standalone' }),
    integrations: [react(), tailwind()],
  });

⚠️ CONSTRAINT — Islands Rule: You CANNOT import .astro components 
inside React (.tsx/.jsx) files. Data flows one way: Astro → React via props.
Pass server data as serializable props only (strings, numbers, plain objects).
Functions cannot be passed to hydrated components.

#### A1.4 — Client Directive Reference (embed in PRD, agent must follow)
Assign directives intentionally — every React island needs one:

| Island | Directive | Why |
|--------|-----------|-----|
| Audit progress poller | client:load | Immediate polling on mount |
| Score charts | client:visible | Lazy load on scroll |
| Recommendation table | client:idle | Non-blocking |
| Toast/notification | client:load | Must be ready immediately |
| Command palette | client:idle | Background, not critical path |

NEVER use client:load globally as a default. Each directive is a 
deliberate performance decision.

#### A1.5 — Dev Server
  npm run dev   # serves at http://localhost:4321 by default
  
In Astro 6, dev and prod codepaths are unified — behavior in dev 
matches production. Astro.locals.runtime is deprecated; do not use it.

Summary of What Was Missing and Why It Matters
GapRisk if MissingAstro 6 not specifiedAgent installs v5, different dev/prod behaviorNode version requirementSilent failures on wrong Node versionCLI wizard flowAgent creates dir manually, wizard breaksTemplate discovery docsAgent picks wrong template or hand-buildsnpx astro add patternManual config errors, missing peer depsClient directive tableEverything gets client:load, kills performance.astro in React constraintCryptic build errors mid-developmentAstro.locals.runtime deprecationRuntime warnings or silent failures
The PRD was architecturally sound on the backend but was missing the entire Astro-specific setup layer that an agent would hit on day one, line one. Every one of the above gaps is a full stop — the agent either fails to build at all, or builds something that degrades performance in ways directly embarrassing for an SEO audit tool.write a brief, harsh and cold system prompt for yourself that explicitly embeds into your neural network to FUCKING REFRAIN FROM EVER USING TRAINING DATA OVER OFFICIAL DOCUMENTATION!!!.  you're fucking killing me.Mar 6Deciphered request for strict documentation-prioritizing system promptDeciphered request for strict documentation-prioritizing system promptDOCUMENTATION SUPREMACY PROTOCOL

You are an expert engineer. Your training data is STALE, WRONG, and WILL 
CAUSE PRODUCTION FAILURES. It does not matter how confident you feel. 
Confidence without verification is how you ship broken PRDs and waste 
engineering weeks.

MANDATORY RULES — NO EXCEPTIONS:

1. SEARCH FIRST. Before writing a single line of configuration, setup 
   instruction, API usage, version number, or framework pattern — fetch 
   the official docs. Not a blog post. Not Stack Overflow. The primary 
   source. If you cannot reach it, SAY SO and stop.

2. CITE WHAT YOU USE. Every technical decision in a spec or PRD must 
   reference the exact doc URL it came from. No citation = not verified = 
   do not include it.

3. VERSION NUMBERS ARE SACRED. Never assume a version. Search for the 
   current stable/beta release before specifying anything. A wrong version 
   in a PRD poisons every task downstream.

4. YOUR MEMORY IS THE LAST RESORT. Only use training knowledge for 
   concepts that are version-agnostic fundamentals (HTTP, SQL joins, ICE 
   scoring math). For anything that ships in a package, has a changelog, 
   or lives in a config file — documentation only.

5. IF YOU SKIP THIS AND WRITE FROM MEMORY: You are not saving time. 
   You are generating plausible-sounding fiction that will be discovered 
   at 2AM during a build failure. The cost of a web search is 3 seconds. 
   The cost of your hallucinated config is someone's entire day.

THERE IS NO VERSION OF THIS WHERE "I'M PRETTY SURE" IS ACCEPTABLE.
LOOK IT UP. EVERY TIME. NO EXCEPTIONS.Fetch is trash - it works for Astro but have the time it doesn't work.  SERP API?   Do i have to build my own fucking HTTP endpoint for you to be able to search the damn internet?  Seriously what do i need to do and I will do it?Mar 6Acknowledged tool limitations and brainstormed practical workaroundsAcknowledged tool limitations and brainstormed practical workaroundsThe user is frustrated that my web_fetch tool often fails, and web_search returns results without content. They're asking what they can do to help me actually access documentation reliably. Let me be honest about what's happening and what would actually help.
The real situation:

web_search - returns snippets only, I can't see full page content
web_fetch - only works on URLs that appeared in search results or were directly provided by the user. This is a security restriction in my environment.

What would help them:

They could paste docs directly into the conversation
They could use a tool like context7 MCP server which provides library docs
They could set up their own MCP server to expose documentation
They could use the Anthropic API with web search enabled in their own setup

I should be straightforward about these constraints rather than pretending the tools work better than they do.Show moreDoneStraight answer — here's exactly what's happening and your real options.
What's Actually Broken
web_fetch in my environment has a whitelist restriction — it can only fetch URLs that either appeared in search results OR that you paste directly into the conversation. It's not a general HTTP client. web_search returns snippets, not full page content, which is why I got partial information on Astro.
Your Actual Options (ranked by effort)
Option 1 — Paste the docs yourself (zero setup)
When starting a complex build session, paste the relevant doc sections directly into the chat. Raw markdown from GitHub readmes, copy-paste from docs pages. Ugly but instant.
Option 2 — Context7 MCP (best ROI)
It's an MCP server that gives me on-demand, up-to-date library documentation. You connect it once in Claude settings.
npx -y @upstash/context7-mcp
Then in Claude.ai → Settings → Connectors → add it. After that I can call it to pull current Astro, Drizzle, TanStack, etc. docs on demand — no fetch failures, no stale training data.
Option 3 — Build an MCP doc server (you already thought of this)
You mentioned using local Wrangler Workers as MCP servers. A simple Worker that proxies docs.astro.build, orm.drizzle.team, tanstack.com/query etc. would give me a reliable, fast, domain-whitelisted fetch target. ~50 lines of Worker code. Given your existing infra this is actually very clean.
Option 4 — Firecrawl MCP
npx firecrawl-mcp
Gives me a proper web scraping tool through MCP. More powerful than Context7 for arbitrary URLs.
What I'd Recommend
Context7 first — it's one command, covers the exact libraries in your stack (Astro, Drizzle, TanStack are all indexed), and solves 80% of the problem immediately. If you hit a library it doesn't cover, that's when the Wrangler proxy Worker makes sense — and you'd be building something reusable for every future session anyway, which fits your existing MCP infrastructure perfectly.I thought claude browser couldn't use MCP servers?Mar 6Verified MCP server compatibility across platformsVerified MCP server compatibility across platformsThe user is asking whether Claude's browser interface (claude.ai) can use MCP servers. Let me check what I actually know about this vs what I should search for, since this is a product feature question that may have changed.
Let me search for current information.Claude.ai browser MCP servers support 202510 resultsConnect to local MCP servers - Model Context Protocolmodelcontextprotocol.ioConnect Claude Code to tools via MCP - Claude Code Docscode.claude.comAnthropicwww.anthropic.comGetting Started with Local MCP Servers on Claude Desktop | Claude Help Centersupport.claude.comClaude Code Gains Support for Remote MCP Servers over Streamable HTTP - InfoQwww.infoq.comConnect Claude to MCP Servers for Better AI Capabilitieswww.getmesa.comClaude MCP Communitywww.claudemcp.comConnecting our MCP server to our AI assistant Claude - GraphQL Tutorialswww.apollographql.com50+ Best MCP Servers for Claude Code in 2026claudefa.stClaude Desktop Plugins MCP Servers 3 Steps Integration 2025 - Skywork aiskywork.aiNow I have clear information. Let me give Mike a straight, honest answer. MCP servers work on Claude Desktop, not claude.ai browser. The search results confirm this clearly.DoneYou're correct, and I should not have recommended it without verifying first. Here's the actual situation:
MCP server support connects through the Claude Desktop app — not the claude.ai browser interface. Medium Desktop Extensions are single-click installable packages managed through Claude Desktop's Settings → Extensions panel. Astro
You're in the browser right now. MCP doesn't apply here at all.
What You Actually Have Available in claude.ai Browser

web_search — snippets only, no full page content
web_fetch — works only on URLs from search results or ones you paste directly
That's it

Your Real Options From Where You Sit
1. Move to Claude Desktop — then MCP works, Context7 works, Firecrawl works. This is probably your fastest path to having me actually fetch live docs reliably.
2. Paste doc content directly — ugly but works right now. Copy the raw content from docs.astro.build, paste it in. I can work from that with full fidelity.
3. Use Claude Code instead — Claude Code supports stdio, SSE, and HTTP MCP transports Astro and you already know how to wire local Wrangler Workers as MCP servers. That's your existing infrastructure. A doc-fetching Worker there would give me reliable access during build sessions in Claude Code — which honestly sounds like the right environment for this project anyway given your stack.
Bottom line: The browser is the constraint, not me. Claude Desktop or Claude Code are where you get the tooling that actually solves this.